#include <iostream>
#include <vector>
#include<map>

using namespace std;
void print(long arr[],int n){
    map<int, int> m; 
}
int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int n;
    cin>>n;
    long arr[n];
    for(int i=0;i<n;i++){
        cin>>arr[i];
    }

    print(arr,n);

}